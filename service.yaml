apiVersion: v1
kind: Service
metadata:
  name: flask-portfolio-service
spec:
  type: LoadBalancer
  selector:
    app: flask-portfolio
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 5000